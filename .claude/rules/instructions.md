1. Always use theme colors mentioned in global.css
2. Always check the existing global.css before adding any inline.
3. Don't ever change global.css. If require, share the code. I'll place it there manually.
3. When required, always use lucide icons from here https://lucide.dev/, the package is already installed.
4. While creating a component always use arrow function style and export it as default.
5. Always refert '.claude\rules\form.tsx' for structure and pattern while creating a form.
6. Validation schema for that form must be placed at 'src\schemas' with a proper name.
